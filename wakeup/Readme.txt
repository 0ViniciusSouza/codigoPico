Código para fazer o wakeup do sistema
Para rodar, tem que mudar o endereço dos nós e a ordem (linhas 59 a 61).

A função de wakeup é chamada no setup e também pode ser chamada se o usuário manda 'r' pelo serial
